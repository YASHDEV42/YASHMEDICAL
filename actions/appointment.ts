"use server";

import { PatientType } from "@/types/User";

const bookAppointment = async (prevState: unknown, formData: unknown) => {
  console.log("formData: ", formData);
};
export { bookAppointment };
